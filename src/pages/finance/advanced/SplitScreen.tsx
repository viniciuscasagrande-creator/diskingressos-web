import React,{useState} from 'react'; import {Box} from './shared';
export default function SplitScreen(){const [beneficiarios,setB]=useState([{nome:'Produtor',tipo:'percentual',valor:90},{nome:'DiskIngressos',tipo:'percentual',valor:10}]);return <div className="fat-page"><header><small>ADVANCED & TAXAS</small><h1>Split Financeiro Automático</h1><p>Configure e simule a distribuição financeira por evento.</p></header><Box title="Regra de Split"><div className="fat-form"><input placeholder="Nome da regra"/><select><option>Percentual</option><option>Valor fixo</option></select><input type="date"/><button>Salvar regra</button></div></Box><Box title="Beneficiários"><div className="fat-table">{beneficiarios.map((b,i)=><div className="fat-row" key={i}><input value={b.nome} onChange={e=>{let x=[...beneficiarios];x[i]={...b,nome:e.target.value};setB(x)}}/><span>{b.tipo}</span><input type="number" value={b.valor} onChange={e=>{let x=[...beneficiarios];x[i]={...b,valor:+e.target.value};setB(x)}}/><b>{b.valor}%</b></div>)}</div><button onClick={()=>setB([...beneficiarios,{nome:'Novo beneficiário',tipo:'percentual',valor:0}])}>+ Beneficiário</button></Box><Box title="Simulação"><div className="fat-kpis"><div><small>Venda</small><strong>Informe o valor</strong></div><div><small>Distribuição</small><strong>Calculada pela regra</strong></div><div><small>Status</small><strong>Prévia</strong></div></div></Box></div>}